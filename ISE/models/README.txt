########################################################################
###
##  File: README.txt
##  Desc: Describes the contents of the Models directory
#

The models directory contains the IseRubyModels used by the 
AadseSimulation.  There is a naming convention used here.

snake_case.rb file names encapsulate the main model component

Directories with CamelCase names contain all the model specific
libraries and plug-in code used by the main model file.

