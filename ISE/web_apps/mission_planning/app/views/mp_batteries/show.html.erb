<% title "Show Battery: " + @mp_battery.name %>

<% render :partial => 'description' %>

<%= render :partial => 'shared/show_model',
           :locals => { :model_class => MpBattery, :model_entry => @mp_battery } %>

<%= render :partial => 'mp_battery_configurations/show',
	   :locals => { :mp_battery_configurations => @mp_battery.mp_battery_configurations } %>

<%= render :partial => 'shared/show_links',
           :locals => { :model_class => MpBattery, :model_entry => @mp_battery } %>
